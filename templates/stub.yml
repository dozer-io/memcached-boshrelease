---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: memcached1
  security_groups:
    - memcached

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: memcached
    version: latest
